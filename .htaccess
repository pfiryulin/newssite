RewriteEngine On

# Если файл или папка существует — не переписываем
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Всё остальное отправляем в index.php
RewriteRule ^ index.php [QSA,L]